"""Federation plane — represent other authority domains, never merge them (Phase 10).

10.1 establishes representation before trust: a remote Nexus may declare identity,
compatibility, and capability claims, but those declarations never become local
authority or shared state. Core-only.
"""
